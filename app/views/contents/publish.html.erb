<%= form_for @content, :url => {:action => @action} do |c| %>
  <%= select('contents', 'location', @locations.collect {|l| [l.owner.name, l.id]} ) %>
  <%= c.submit I18n.t('.submit'), id: 'submit'%>
<% end %>
