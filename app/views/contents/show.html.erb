<article>
  <hgroup>
    <h1>ID: <%= @content.id %></h1>
    <% if @content.parent %>
      <h3><%= I18n.t('.in_reply_to') %>: <%= link_to @content.parent.text, @content.parent %></h3>
    <% end %>
    <%= render partial: 'contents/tag_content' %>
  </hgroup>
  <p id="description"><%= @content.description %></p>
  <section>
    <h4><%= I18n.t('.tags') %></h4>
    <% @content.tag_list.each do |tag|%>
      <p><%= tag %></p>
    <% end%>
  </section>
  <section id="replies">
    <h4><%= I18n.t('.replies') %></h4>
    <% if @content.children %>
      <%= render partial: 'contents/content', collection: @content.children %>
    <% else %>
      <p><%= I18n.t('.no_replies') %></p>
    <% end %>
    <footer>
      <%= render 'manage_content' %>
    </footer>
  </section>
</article>