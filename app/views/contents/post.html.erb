<%= form_for @content, :url => {:action => 'post'} do |c| %>
  <%= select('contents', 'location', @locations.collect {|l| [l.user.email, l.id]} ) %>
  <%= c.submit I18n.t('.submit'), id: 'submit'%>
<% end %>